inds <- seq(1,31,by=2)

for (i in inds){

    temp <- a[,i:(i+1)];
    nam  <- names(temp)[1];
    names(temp) <- c("L","B");
    temp <- na.omit(temp);
    write.csv(temp,file=paste(paste("Pike_11",nam,sep=""),".csv", sep=""),row.names=FALSE);

}
